FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV DJANGO_SETTINGS_MODULE=smart_tasks.settings

EXPOSE 8000
CMD ["gunicorn", "smart_tasks.wsgi:application", "--bind", "0.0.0.0:8000"]
